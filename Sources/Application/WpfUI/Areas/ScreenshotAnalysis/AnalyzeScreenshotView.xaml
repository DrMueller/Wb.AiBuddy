<UserControl x:Class="Mmu.Wb.TimeBuddy.WpfUI.Areas.ScreenshotAnalysis.AnalyzeScreenshotView"
			xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			xmlns:controls="clr-namespace:Mmu.Mlh.WpfCoreExtensions.Areas.MvvmShell.CommandManagement.Components.CommandBars.Controls;assembly=Mmu.Mlh.WpfCoreExtensions"
			mc:Ignorable="d"
			d:DesignHeight="450" d:DesignWidth="800">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<controls:CommandBar Commands="{Binding Path=Commands, Mode=OneWay}" />

		<GroupBox Grid.Row="1" Header="Input">
			<TextBox
				TextWrapping="Wrap"
				AcceptsReturn="True"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				MinHeight="600"
				VerticalScrollBarVisibility="Visible"
				Text="{Binding AnalysisInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		</GroupBox>

		<GroupBox Grid.Row="2" Header="Result">
			<TextBox
				TextWrapping="Wrap"
				AcceptsReturn="True"
				HorizontalAlignment="Stretch"
				VerticalAlignment="Stretch"
				MinHeight="600"
				VerticalScrollBarVisibility="Visible"
				Text="{Binding AnalysisResult, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
		</GroupBox>
	</Grid>
</UserControl>